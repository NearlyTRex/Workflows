# Tools the reusable workflows run. Regenerate requirements.txt after editing:
#   pip-compile --generate-hashes --allow-unsafe -o requirements.txt requirements.in
zizmor
pip-audit
ruff
shellcheck-py
